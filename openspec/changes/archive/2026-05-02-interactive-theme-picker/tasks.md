## 1. CLI Picker

- [x] 1.1 Add an injectable interactive theme picker seam to `OmuxCLICommand`.
- [x] 1.2 Implement a dependency-free raw-mode terminal picker for TTY stdin/stdout.
- [x] 1.3 Preserve the current typed number/name fallback for non-TTY contexts.

## 2. Tests and Docs

- [x] 2.1 Add CLI tests for interactive picker selection through the injected seam.
- [x] 2.2 Keep/update tests for explicit name, typed fallback, cancellation, and theme listing behavior.
- [x] 2.3 Update CLI documentation for the interactive picker.
- [x] 2.4 Run OpenSpec validation and the relevant Swift tests.
